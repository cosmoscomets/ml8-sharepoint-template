{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ml8.example/schemas/sharepoint-site.json",
  "title": "ML8 SharePoint site configuration",
  "type": "object",
  "required": ["siteTitle", "siteUrl", "template", "page", "libraries", "quickLinks", "importantDates"],
  "properties": {
    "$schema": { "type": "string" },
    "siteTitle": { "type": "string", "minLength": 1 },
    "siteUrl": { "type": "string", "pattern": "^https://" },
    "template": { "type": "string", "enum": ["document-hub", "landing-page"] },
    "heroImage": {
      "type": "object",
      "required": ["file", "alt"],
      "properties": {
        "file": { "type": "string", "minLength": 1 },
        "alt": { "type": "string", "minLength": 1 }
      },
      "additionalProperties": false
    },
    "logo": {
      "type": "object",
      "required": ["file", "alt"],
      "properties": {
        "file": { "type": "string", "minLength": 1 },
        "alt": { "type": "string", "minLength": 1 }
      },
      "additionalProperties": false
    },
    "accentColor": { "type": "string", "pattern": "^#[0-9A-Fa-f]{6}$" },
    "heroCta": {
      "type": "object",
      "required": ["label", "url"],
      "properties": {
        "label": { "type": "string", "minLength": 1 },
        "url": { "type": "string", "minLength": 1 }
      },
      "additionalProperties": false
    },
    "page": {
      "type": "object",
      "required": ["name", "title", "description"],
      "properties": {
        "name": { "type": "string", "pattern": "^[A-Za-z0-9-]+$" },
        "title": { "type": "string", "minLength": 1 },
        "description": { "type": "string" }
      },
      "additionalProperties": false
    },
    "libraries": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["title", "url", "description"],
        "properties": {
          "title": { "type": "string", "minLength": 1 },
          "url": { "type": "string", "pattern": "^[A-Za-z0-9-]+$" },
          "description": { "type": "string" }
        },
        "additionalProperties": false
      }
    },
    "quickLinks": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["label", "url"],
        "properties": {
          "label": { "type": "string", "minLength": 1 },
          "url": { "type": "string", "minLength": 1 }
        },
        "additionalProperties": false
      }
    },
    "importantDates": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    }
  },
  "additionalProperties": false
}
